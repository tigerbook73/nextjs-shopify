# CLAUDE.md

本文件为 Claude Code（claude.ai/code）提供本仓库的开发指南。

@AGENTS.md

---

> **重新生成说明（针对 `/init` 命令）**：本文件采用双文件结构——`AGENTS.md` 存放所有 AI agent 通用的内容，`CLAUDE.md` 通过 `@AGENTS.md` 引入后只追加 Claude Code 专属内容。重新生成时，请保持此结构：公共内容更新到 `AGENTS.md`，Claude Code 专属内容留在 `CLAUDE.md`，不得将两个文件合并或用单文件覆盖。所有描述性文字使用中文撰写，文件名、变量名、命令、代码保持英文。重新生成时还需参考 `.claude/claude-requirements.md`，确保各工具集所需章节在 AGENTS.md 中完整存在。
